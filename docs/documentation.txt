Przyklad wykorzystania hintow:

 FCML_I32( "call far fword ptr [ebx+00000001h]", 0xFF, 0x5B, 0x01 );
 
 moze byc takze assemblowany do NEAR call, w tym przypadku hint jednoznacznie dobiera kodowanie.
 
 Hinty dla operandow, przyklad hintu dla operandu z operujacego na media-SIMD:
 
 FCML_I64_A_P( "movq mm2,mmword ptr [rax]", 0x48, 0x0F, 0x6E, 0x10 );
 
 ---
 
 Opisac fraze "Outside IA-32e mode, the register operand is always 32 bits, regardless of the value of CS.D; in 64-bit mode, the
register operand has 64 bits (the instruction cannot be executed in compatibility mode)."

W naszym rzypadku steruje sie tym za pomoca address_mode 32_bit lub 64_bit.
 
 INVEPT
 
 Featurs:
 branch hints dla JCC
 
 Tam gdzies jest lista features:
 http://www.godevtool.com/GoasmHelp/GoAsm.htm#why
 
 Zaznaczyć ze nie rozrozniamy virtual 8086 oraz real-mode addressing wiec podcaas assemblacji nie sprawdzamy tej graniy 1MB przy adresowaniu linowym czyli przy 0x67 zmienijącym tryb adresowania na 32 bitowy w real mode.
 
 Dodac informację, że w przypadku DIV należy podawać także ax, eax omimo tego że jest explicite zdefiniowany!
 
 
 Opisac w hintach INDIRECT addressing, w jaki spsob sie powinno uzywac. Ze w przypadku INTEL jest zupelnie zbedna co najeyzej mozemy wybrac konkretny atryb adresowana
 poneiwaz instnieje tam nadmiarowosc z far addressing 16 bitowym. Opisac jak jesyt uzywany w GAS i ze tam jest wymagany do assemblacji INDIRECT.